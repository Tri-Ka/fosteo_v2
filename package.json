{
  "name": "fosteo",
  "version": "1.0.0",
  "scripts": {
    "sass": "sass --style=compressed --source-map sass:css",
    "sass:watch": "sass --watch --style=compressed --source-map sass:css"
  },
  "devDependencies": {
    "sass": "^1.70.0"
  }
}